name: b11-workflow

on: push

jobs:
  first:
    runs-on: ubuntu-latest

    steps:
      - name: print message
        run: echo "hello Ashpak bro"

      - name: show content of /etc/passwd
        run: sudo cat /etc/passwd